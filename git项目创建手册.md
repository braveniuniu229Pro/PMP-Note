## 一个简要的仓库创建手册
先区分两个不同的建立远程分支追踪关系的命令：
1. git push -u origin（远程名字，需要先添加这个远程仓库） main（本地分支名，不加后面的内容默认同名）[:远程分支名]（可选，不建议不同名）
**这里的逻辑是首次提交远程的时候，和这个远程分支建立追踪，如果远程还没有这个分支（空的远程仓库）就创建这个分支并建立追踪关系完成提交。
2. git branch --set-upstream-to=origin/main（远程分支） main（本地分支）
**这里建立追踪关系的前提条件是，远程仓库已经创建了这个分支，所以在空仓库的时候会导致失败。**

基于以上两个命令，那么可以有两个标准的操作流程：

**推荐**
1. 从远程仓库开始建立项目：
在git上创建好项目，然后直接在本地使用git clone建立本地仓库

2. 从本地仓库开始建立项目：
   1. 首先创建项目文件夹，然后git init
   2. 编辑内容，完成第一次本地的提交
   3. 创建远程仓库，**一定注意不要添加任何内容如readme**，否则会产生一次根提交，导致本地和远程根不同无法正常提交。
   >这时 使用git fetch origin拉去远程仓库
   再进行git merge origin/main会产生冲突，因为他们的根提交是不同的如远程是R，本地是L
   会提示fatal: refusing to merge unrelated histories
   这时可以使用git merge origin/main --allow-unrelated-histories来实现合并，产生的效果就是一个新提交m，然后父节点时L和R，这是非常不好的，相当于根就是分叉的有两个。
   4. 直接使用刚才说的push -u origin main去建立追踪关系
3. git branch --set-upstream-to=这个命令可以用于远程已经建立了一个新的分支如dev，然后可以在本地使用
git switch -c dev（创建并切换到这个分支），然后和远程建立追踪关系，进行拉取或是上传的操作等。
